# Диагностика проблемы с Teachers

## 🔍 ТЕКУЩАЯ ПРОБЛЕМА: GET запрос возвращает 403, POST работает

**Статус:** В процессе диагностики

**Симптомы:**
- ❌ GET `/api/v1/admin/teachers` возвращает 403 Forbidden с пустым телом ответа
- ✅ POST `/api/v1/admin/create-teacher` работает нормально (email отправляется)
- ✅ Токен валиден и содержит роль `["ADMIN"]`
- ✅ `hasAdminRole: true`

**Следующие шаги для диагностики:**
1. Выполните в консоли: `await apiService.debug.testAllTeacherEndpoints()`
2. Проверьте логи backend на наличие Spring Security ошибок
3. Убедитесь, что GET и POST имеют одинаковую конфигурацию безопасности

**Возможные причины:**
- Backend может иметь разные настройки безопасности для GET и POST методов
- Проблема с CORS для GET запросов
- Эндпоинт GET требует дополнительных прав, которых нет у POST

---

## ✅ РЕШЕНО: Проблема с циклической зависимостью JSON

**Основная проблема:** Backend возвращал некорректный JSON из-за циклической зависимости между entities User и Subject.

### Исправления:

1. **Устранена циклическая зависимость в entities:**
   - Добавлен `@JsonIgnore` на поле `User.subjects`
   - Добавлен `@JsonIgnore` на поле `Subject.users`
   - Добавлен `@JsonIgnore` на поле `User.password` (для безопасности)

2. **Исправлен маппинг полей** - Backend возвращает:
   - `surName` (вместо `surname`)
   - `lastName` (вместо `lastname`)
   - `subject_id` и `subject_name` (вместо `id` и `name`)

3. **Добавлено подробное логирование** - Теперь в консоли браузера выводится:
   - Информация о текущем пользователе
   - Токен и его payload (включая роли)
   - Детали запроса к API
   - Данные полученных преподавателей

4. **Улучшена обработка ошибок** - Показывается, если есть проблема с правами доступа (403 Forbidden)

## Как проверить, что все работает

### 1. Откройте консоль браузера (F12)

### 2. Перейдите на страницу Teachers

### 3. Проверьте логи в консоли:

```
=== Информация о текущем пользователе ===
User: {...}
Token: eyJhbGciOiJ...
Token payload: {...}
Roles/Authorities: ["ADMIN"] или ["ROLE_ADMIN"]
=========================================

=== Начинаем загрузку преподавателей ===
API: Запрашиваем список преподавателей...
API: Диагностика токена...
...
```

## Возможные проблемы и решения

### Проблема 1: Ошибка 403 Forbidden
**Причина:** У пользователя нет прав ADMIN

**Решение:**
1. Проверьте, что в Token payload есть authority "ADMIN"
2. Если нет, нужно войти под пользователем с правами администратора
3. В базе данных проверьте таблицу `user_authorities`, должна быть запись с `authority = 'ADMIN'`

### Проблема 2: Ошибка 401 Unauthorized
**Причина:** Токен истек или невалиден

**Решение:**
1. Выйдите и войдите заново
2. Проверьте, что токен не истек (exp в payload должен быть больше текущего времени)
3. Используйте функцию `apiService.diagnoseToken()` в консоли

### Проблема 3: Данные не отображаются (пустой массив)
**Причина:** В базе данных нет преподавателей

**Решение:**
1. Проверьте логи: "Количество преподавателей: 0"
2. Создайте преподавателя через форму "Добавить преподавателя"
3. Проверьте в базе данных наличие пользователей с authority = 'TEACHER'

### Проблема 4: CORS ошибка
**Причина:** Backend не разрешает запросы с фронтенда

**Решение:**
1. Убедитесь, что backend запущен на `http://localhost:8080`
2. Проверьте SecurityConfiguration в backend - должны быть разрешены `http://localhost:3000`
3. Проверьте логи backend на наличие CORS ошибок

## Полезные команды для отладки в консоли браузера

```javascript
// Проверить токен
apiService.diagnoseToken()

// Получить текущий токен
apiService.debug.getToken()

// Очистить токен
apiService.debug.clearToken()

// Проверить роли в токене
apiService.debug.checkRoles()

// Тест запроса к teachers
await apiService.debug.testTeachers()

// Полная диагностика
await apiService.debug.fullDiagnostic()

// Проверить все админские эндпоинты
await apiService.testAdminEndpoints()

// 🆕 Детальная диагностика конкретного запроса
await apiService.debug.diagnoseRequest('/api/v1/admin/teachers')

// 🆕 Тест всех возможных эндпоинтов для teachers
await apiService.debug.testAllTeacherEndpoints()

// 🆕 Прямой fetch (обход ApiService.request)
await apiService.debug.directFetch('/api/v1/admin/teachers')
```

## Структура данных от API

### GET /api/v1/admin/teachers
```json
[
  {
    "name": "Иван",
    "surName": "Иванов",
    "lastName": "Иванович",
    "email": "ivan@example.com",
    "authorities": [
      {
        "authority": "TEACHER"
      }
    ],
    "passwordTemporary": false,
    "subjects": [
      {
        "subject_id": 1,
        "subject_name": "Математика"
      }
    ]
  }
]
```

### GET /api/v1/admin/subjects
```json
[
  {
    "subject_id": 1,
    "subject_name": "Математика"
  }
]
```

## Проверка прав доступа в Backend

Эндпоинт `/api/v1/admin/teachers` требует authority `ADMIN`:

```java
.requestMatchers("/api/v1/admin/**").hasAnyAuthority(Authorities.ADMIN.name())
```

Убедитесь, что пользователь имеет эту роль в базе данных.

